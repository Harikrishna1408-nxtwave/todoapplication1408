import { EmptyTodoContainer } from "./styledComponents";

const EmptyTodo=() => {
    return (
        <EmptyTodoContainer>    
        <h1>No Todos Available</h1>
        </EmptyTodoContainer>
    );
}

export default EmptyTodo;